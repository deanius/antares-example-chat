{
  "name": "antares-example-chat",
  "private": true,
  "scripts": {
    "start": "meteor run --port 3333",
    "deploy": "git push heroku master"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.0"
  }
}
